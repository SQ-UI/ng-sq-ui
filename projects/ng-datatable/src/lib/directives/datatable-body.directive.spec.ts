import { DatatableBodyDirective } from './datatable-body.directive';

describe('DatatableBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new DatatableBodyDirective();
    expect(directive).toBeTruthy();
  });
});
