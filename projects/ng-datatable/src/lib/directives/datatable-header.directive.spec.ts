import { DatatableHeaderDirective } from './datatable-header.directive';

describe('DatatableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new DatatableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
